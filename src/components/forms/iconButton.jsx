import { Button } from 'antd'
import React from 'react'

const IconButton = () => {
  return (
    <Button type="primary" 
    // icon={<DownloadOutlined />} 
    size={'middle'}>
    Download
  </Button>
  )
}

export default IconButton