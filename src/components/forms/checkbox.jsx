import { Checkbox } from "antd"

const FormCheckbox = () => {
  return (
    <Checkbox
    className=""
    key={''}
    onChange={() => console.log('check')}
  >
    <h1>Baleno (1234)</h1>
  </Checkbox>
  )
}

export default FormCheckbox